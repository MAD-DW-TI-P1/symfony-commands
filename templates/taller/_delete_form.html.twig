<form method="post" action="{{ path('app_taller_delete', {'id': taller.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taller.id) }}">
    <button class="btn">Delete</button>
</form>
